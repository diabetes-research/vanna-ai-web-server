# OpenAI API Key used for accessing GPT-3.5 Turbo model
OPENAI_API_KEY="<replace_with_your_openai_api_key>"

# Name of the GPT-3.5 Turbo model
MODEL_NAME="<replace_with_model_name>"

# Hostname of the remote server
REMOTE_HOST="<replace_with_remote_host>"

# Password for remote server access
REMOTE_PASSWD="<replace_with_remote_password>"

# Username for remote server access
REMOTE_UNAME="<replace_with_remote_username>"

# Name of the PostgreSQL database
DB_NAME="<replace_with_database_name>"

# Port number for PostgreSQL connection
PORT="<replace_with_port_number>"

# List of schema names within the database
SCHEMA_NAMES=["<replace_with_schema_names>"]
